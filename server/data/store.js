// Temporary data store for users, files, and subjects
const users = [];
const files = [];
const subjects = {}; // Map userId to an array of subjects for each user

module.exports = { users, files, subjects };
